---
id: stdlib-math
title: Math
nav: Math
group: Standard Library
---

::fn-group
::fn abs(n)
::fn floor(n) / ceil(n) / round(n)
::fn sqrt(n) / pow(b, e) / sign(n)
::fn sin(n) / cos(n) / tan(n)
::fn arcsin(n) / arccos(n) / arctan(n)
::fn arctan2(y, x)
::fn log2(n) / log10(n) / ln(n) / exp(n)
::fn min(a, b) / max(a, b)
::fn clamp(v, lo, hi) / lerp(a, b, t)
::fn random(n) / irandom(n)
::fn random_range(lo, hi) / irandom_range(lo, hi)
::fn distance(x1, y1, x2, y2) / angle_between(x1, y1, x2, y2)
::fn deg_to_rad(d) / rad_to_deg(r)
::fn-group-end

### Constants

::constants PI INF NAN LAMBDA_VERSION
