---
id: stdlib-string
title: String
nav: String
group: Standard Library
---

::fn-group
::fn str_length(s) → number
::fn str_upper(s) / str_lower(s)
::fn str_sub(s, start, len) → string
::fn str_pos(needle, haystack) → number
::fn str_replace(s, from, to) → string
::fn str_char_at(s, i) → string
::fn str_ord(c) → number / str_chr(n) → string
::fn str_trim(s) / str_repeat(s, n)
::fn str_pad_left(s, width, char) / str_pad_right(s, width, char)
::fn str_split(s, sep) → array
::fn str_starts_with(s, prefix) → bool
::fn str_ends_with(s, suffix) → bool
::fn str_contains(s, sub) → bool
::fn-group-end
