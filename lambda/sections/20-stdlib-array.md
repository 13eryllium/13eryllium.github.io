---
id: stdlib-array
title: Array
nav: Array
group: Standard Library
---

::fn-group
::fn array_length(arr) → number
::fn array_push(arr, v) / array_pop(arr)
::fn array_insert(arr, i, v) / array_delete(arr, i)
::fn array_slice(arr, start [, end]) → array
::fn array_reverse(arr) / array_sort(arr)
::fn array_sort_fn(arr, func(a,b){...})
::fn array_join(arr [, sep]) → string
::fn array_contains(arr, v) → bool
::fn array_find(arr, v) → number
::fn array_map(arr, fn) / array_filter(arr, fn)
::fn array_each(arr, fn)
::fn array_reduce(arr, fn, init)
::fn array_flat(arr) → array
::fn-group-end
