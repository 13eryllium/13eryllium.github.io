---
id: variables
title: Variables
nav: Variables
group: Language
---

All variables are declared with `let`. Lambda is dynamically typed - a variable can hold any value type.

```lambda
let x = 10;
let name = "hello";
let flag = true;
let nothing = null;
```

Variables are scoped to the block they're declared in. There is no hoisting.
